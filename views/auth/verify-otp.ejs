<%
  pageCss = 'booking.css';
%>

<section style="padding-top: 120px; padding-bottom: var(--section-gap);">
  <div class="container" style="max-width: 500px; margin: 0 auto;">
    
    <div style="text-align: center; margin-bottom: 48px;">
      <span class="section-label reveal">Xác thực</span>
      <h1 class="section-heading reveal" style="font-size: clamp(28px, 4vw, 40px);">Mã OTP</h1>
      <p class="body-text reveal" style="margin-top: 12px;">Mã xác thực đã được gửi tới email <strong><%= email %></strong></p>
    </div>

    <div class="contact-form-section">
      <form method="POST" action="/auth/verify-otp" novalidate>
        <div class="form-group">
          <label for="email">Email</label>
          <input type="email" id="email" name="email" value="<%= email %>" readonly style="background-color: rgba(0,0,0,0.02);">
        </div>

        <div class="form-group">
          <label for="otp">Mã OTP (6 chữ số) <span class="form-required">*</span></label>
          <input type="text" id="otp" name="otp" placeholder="123456" required autocomplete="off" maxlength="6" style="letter-spacing: 4px; text-align: center; font-size: 20px; font-weight: 500;">
        </div>

        <button type="submit" class="btn btn-dark submit-btn" style="width: 100%; margin-top: 24px;">
          <span>Xác thực →</span>
        </button>
      </form>
      
      <div style="text-align: center; margin-top: 24px;">
        <p style="font-size: 14px; color: var(--smoke);">
          Chưa nhận được mã? <a href="/auth/register" style="color: var(--logo-mocha); font-weight: 500;">Đăng ký lại</a>
        </p>
      </div>
    </div>

  </div>
</section>
